#+title: Induction

+ *Exercise:* A heap is a binary tree in which every parent node’s value is greater than or equal to its children’s values. Write this definition using inference rules.

  #+begin_src

------ n number
n heap


n_1 p_1 heap  n_2 p_2 heap
-------------------------- (n_0 >= n_1) and (n_0 >= n_2)
n (p_1, p_2) heap
  #+end_src


+ *Exercise*: Prove that the root of a heap has the maximum value of all of its nodes’ values.

  For

  #+begin_src
------
n heap
  #+end_src

  n is the maximum. And for

  #+begin_src
n_1 p_1 heap  n_2 p_2 heap
--------------------------
n_0 (p_1, p_2) heap
  #+end_src

  There is $n_0$ is greater than or equal to $n_1$, and $n_2$. The same with the branches.
